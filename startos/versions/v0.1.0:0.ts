import { VersionInfo, IMPOSSIBLE } from "@start9labs/start-sdk";
import { sdk } from "../sdk";

export const v0_1_0__0 = VersionInfo.of({
  version: "0.1.0:0",
  releaseNotes: "Initial release",
  migrations: {},
});
